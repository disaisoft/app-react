import React from 'react';
import Main from './Main';

const Info = ({children}) => (
    <div className="Info">
        <div className="Info__container">
            {children}
        </div>
    </div>
);

export default Main;